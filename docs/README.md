# Documentação Docusaurus

```bash
npm ci
npm start
```

Edite as páginas Markdown em `docs/` e coloque imagens em `static/img/`. Execute `npm run build` antes de abrir um pull request para validar links, front matter e MDX.
